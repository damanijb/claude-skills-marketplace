{
  "mcpServers": {
    "bloomberg": {
      "command": "python",
      "args": ["-m", "fastmcp", "run", "${CLAUDE_PLUGIN_ROOT}/server/server.py"],
      "env": {
        "PYTHONPATH": "${CLAUDE_PLUGIN_ROOT}/server"
      }
    }
  }
}
